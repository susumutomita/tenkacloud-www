node_modules/
dist/
coverage/

# Cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# stray tool backups
*.bak

# Terraform local secrets (CLOUDFLARE_API_TOKEN etc.) — never commit
.env.terraform

# Terraform (local state on a trusted machine; never commit it)
.terraform/
*.tfstate
*.tfstate.*
crash.log
terraform.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
